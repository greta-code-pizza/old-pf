import { wall } from "../src/wall";

describe('wall', () => {
    it('retourne le bon nombre de messages', () => {
        // CODE HERE

        /*
         * - Déclarer un tableau de messages
         * - Déclarer un nouveau message
         * - Vérifier que la fonction wall retourne le bon nombre de messages
         */
    });

    it('retourne le dernier message en premier', () => {
        // CODE HERE

        /*
         * - Déclarer un tableau avec plusieurs messages
         * - Déclarer un nouveau message
         * - Vérifier que le premier message du tableau que retourne wall soit
         * bien un nouveau message
         */
    });
});