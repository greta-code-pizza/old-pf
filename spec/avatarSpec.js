import { avatar } from "../src/avatar";

describe('avatar', () => {
    it("retourne 'happy' lorsqu'il y a trop de messages", () => {
        // CODE HERE

        /*
         * - Déclarer un tableau de moins de 10 messages
         * - Vérifier que la fonction avatar retourne 'happy'
         */
    });

    it("retourne 'angry' quand il y a trop de messages", () => {
        // CODE HERE

        /*
         * - Déclarer un tableau de plus de 9 messages
         * - Vérifier que la fonction avatar retourne 'angry'
         */
    });
});